# Sauvegardes de thèmes Shopify — boutique GOMU (gomudaily.com)

## `2026-08-10-gomu-v6-203413225817/`

Copie intégrale du thème **« GOMU v6 — LP sophistiquée (aperçu) »**
(`gid://shopify/OnlineStoreTheme/203413225817`), qui était le thème publié
au 10 août 2026, prise juste avant la refonte de direction artistique
bordeaux/pêche.

486 fichiers, téléchargés via l'API Admin GraphQL (`theme.files`).
`_manifest.tsv` liste `filename`, `size` et `checksumMd5` tels que renvoyés
par Shopify — c'est la référence d'intégrité.

Vérification au moment de la sauvegarde : 430 fichiers avec MD5 strictement
identique, 46 avec l'en-tête de commentaire auto-généré que l'API ajoute à la
lecture (`/* … auto-generated … */`), 10 fichiers JSON re-sérialisés par
l'API (JSON valide, contenu équivalent, formatage différent). Aucun fichier
manquant.

## Restauration

Le chemin normal ne passe pas par ces fichiers : le thème v6 existe toujours
dans la boutique en `UNPUBLISHED` et n'a jamais été modifié. Pour revenir en
arrière, il suffit de le republier (admin → Boutique en ligne → Thèmes →
Publier, ou `themePublish`).

Cette copie locale sert d'archive de dernier recours, si le thème venait à
être supprimé côté Shopify.
